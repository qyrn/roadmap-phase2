
## Day 5 — 17/05 — Phase 2

* Time: 1h

* Did:
 * Anki -> 11/20
 * Construis un Web Scanner
 * Quiz Manuel et Hebdo (6.5/8 pour le manuel et le hebdo 4/5 j'ai eu une question hors scope sur la défense SQLi)

* Learned:
* J'ai crée un dossier scanner, avec un init.py, trois fichiers spécialisés dedans comme fetcher.py, parser.py, reporter.py et un main.py à la racine qui orchestre
 * Reappris à construire du code python en utilisant des fonctions comme def, return et les param, des dictionnaires .get(), .items() et des boucles for, des if/else, des  f-strings, des imports depuis un package
 * Setup un venv et requirements.txt (`python -m venv` + `source venv/bin/activate` et `pip freeze`)
 * Savoir comment import requests et l'utiliser dans un scanner, en utilisant requests.get(url) pour récuperer une réponse HTTP et .headers pour avoir les headers
 * Construire un reporter, qui parcourt un dictionnaire de résultats et différencie l'absence ou la présence avec des préfix adapté à chacun via la condition if/else Le parser retourne un dictionnaire Python de la forme `{nom_header: valeur}` si un header attendu n’est pas trouvé dans le fichier, sa valeur est `"Not Found"`.
 
* Blocked on:
 
* Tomorrow:
 * Semaine 3 : 
   * Gandalf niveau 0 -> 4
   * Reflected XSS Lab 1 et 2
